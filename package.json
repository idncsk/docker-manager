{
  "name": "docker-tray",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "dev": "npx tsup ./src --watch --onSuccess \"npx electron dist\" ",
    "start": "npx electron dist",
    "compile": "npx tsup src",
    "build": "npm run compile && electron-packager . docker-manager --platform=linux --arch=x64"
  },
  "dependencies": {
    "electron": "^23.1.1",
    "electron-context-menu": "^3.6.1"
  },
  "devDependencies": {
    "electron-packager": "^17.1.1",
    "tsup": "^6.6.3",
    "typescript": "^4.5.4"
  }
}
